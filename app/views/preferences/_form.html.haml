= simple_form_for(@preference) do |f|
  = f.error_notification

  .form-inputs
    = f.association :subject
    = f.association :theme
    = f.association :delivery

  .form-actions
    = f.button :submit
